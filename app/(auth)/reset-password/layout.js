export default function Layout({ children }) {
  return (
    <>{children}</>
  )
}
export const metadata = {
  title: "Reset Password - Change Your PKey Password",
  description: "Reset your password securely to regain access to your PKey account.",
};